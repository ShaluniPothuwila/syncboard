# Base URL of the deployed API, including the /api suffix.
# The websocket origin is derived from this by stripping /api.
# Required for production builds; falls back to localhost in dev.
VITE_API_URL=http://localhost:4000/api
