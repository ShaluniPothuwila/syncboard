{
  "name": "syncboard",
  "private": true,
  "description": "Root manifest for the Vercel frontend deployment. The app lives in client/ (deployed to Vercel) and server/ (deployed to Render); this exists so the platform sees a Node project at the repo root.",
  "engines": {
    "node": ">=20"
  }
}
